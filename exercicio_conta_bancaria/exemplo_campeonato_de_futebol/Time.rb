class Time
    def initialize(posição,pontuação)
        @posicão = posicão
        @pontuação = pontuação
    end

    def posicão
        @posicão
    end

    def pontuação
        @pontuação
    end
end
